server:
  port: 8222
spring:
  datasource:
    username: sa
    password: welcome
    url: jdbc:h2:mem:accounts_db
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
    properties:
      hibernate:
        format_sql: false

# actuator endpoints
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
